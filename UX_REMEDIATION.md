# MiniBgm 体验排雷与交互治理专项计划 (UX & Functional Remediation Plan)

> **核心宗旨**：先治病，再健身；不打扰、不割裂、可撤销。  
> **制定时间**：2026-09  
> **状态**：执行中 (Active)

---

## 🧭 为什么需要这份“治病计划”？

在开发客户端的过程中，最容易犯的错误就是**“功能不断堆砌，但基础体验处处卡脖子”**。  
例如：连按 15 次才能补完打卡、点播放按钮跳去网页要求重新登录、打开首页被几十部不认识的番剧轰炸、想看单集吐槽却被一脚踢出客户端跳进浏览器……

这些不是“缺少功能”，而是**“已存在的功能设计反直觉、不合理”**（UX Debt）。  
如果不优先整治这些病症，继续往上加新模块只会让应用更加臃肿、破绽百出。本计划专门用于**排雷现有交互痛点，给出明确的技术处方与分期验收标准**。

---

## 📋 体验病症总览与优先级矩阵

| 序号 | 痛点现象 (Symptom) | 严重等级 | 核心成因 | 目标里程碑 | 状态 |
| :--- | :--- | :---: | :--- | :---: | :---: |
| **01** | **补番打卡繁琐，误触无撤销** | **P0** | 逐集单独请求，无批量“看到此集”，无操作反悔机制 | **v0.3.0** | 已完成 |
| **02** | **播放源跳网页/Web 壳极其割裂** | **P0** | 简单调用浏览器，未探测并唤起本地已装官方播放 App | **v0.3.0** | 已完成 |
| **03** | **日程主页“主客倒置”，信息过载** | **P0** | 首页默认倾泻全网开播，用户收藏追番被淹没 | **v0.3.0** | 已完成 |
| **04** | **卡片色彩饱和度过高，喧宾夺主** | **P0** | 容器颜色重于二次元封面本身，缺少呼吸感与规范 | **v0.3.0** | 已完成 |
| **05** | **单集吐槽与讨论强行跳出客户端** | **P1** | 缺少原生单集评论流，点进讨论只能跳网页版 | **v0.4.0** | 待开始 |
| **06** | **AI 助手缺少“当前页面感知”** | **P1** | 路由未携带上下文，在条目页唤醒 AI 无法识别当前番剧 | **v0.4.0** | 待开始 |
| **07** | **搜索别名容错极弱，返回位置丢失** | **P2** | 依赖接口直搜缺少别名映射，列表未保存滚动偏移 | **v0.5.0** | 待开始 |
| **08** | **弱网异常缺乏局部降级与重试** | **P2** | 异常全屏空白报错，未结合 Room 本地离线缓存展示 | **v0.5.0** | 待开始 |

---

## 💊 专项诊断与技术处方 (Prescriptions & Tech Specs)

### 🏥 战役 01：单集打卡体验重构（高频核心操作）

#### 1. 病症表现
- 用户补番（例如一口气补了 12 集），必须在界面上**连续点按 12 次**打卡，每一次点按都触发一次独立的网络更新，极易触发网络节流或状态乱跳。
- 误触点击某一集打卡后，缺少“撤销 (Undo)”机制，想要改回未看状态非常别扭。

#### 2. 治疗处方
- **长按“看到此集”**：
  - 在剧集列表（`SubjectEpisodesSection`）中，**长按**任意尚未观看的剧集，弹出轻量确认 Sheet/Dialog：“标记看到第 X 集（共 X 集）？”
  - 确认后，一次性将 `1..X` 集批量乐观更新（Optimistic Update），并在后台合并提交至 Bangumi API。
- **4 秒撤销浮条 (Undo Snackbar)**：
  - 用户打卡或批量打卡成功后，底部立即弹出悬浮 `Snackbar(text = "已标记至第 X 集", action = "撤销")`，停留 4 秒。
  - 用户点击撤销，立刻恢复上一次的集数状态，并触发异步回滚。

#### 3. 涉及模块与代码范围
- `:feature:subject` (`SubjectEpisodesSection.kt`, `SubjectDetailViewModel.kt`)
- `:core:data` (`CollectionRepository.kt` 支持批量打卡与带原状态记录的撤销操作)
- `:core:database` (EpisodeEntity 本地批量事务更新)

---

### 🏥 战役 02：播放源唤起原生化（告别 Web 壳断层）

#### 1. 病症表现
- 用户在排期表或番剧详情中点击播放源（如 Bilibili、巴哈姆特、爱奇艺等），应用直接打开了手机浏览器或应用内 WebView。
- 导致：**没有登录状态、看不了 1080P/4K、没有弹幕、还要看网页端广告**，完全失去了现代 Android 客户端的顺滑体验。

#### 2. 治疗处方
- **打造原生 Deep Link 分发器 (`StreamingIntentResolver`)**：
  - 自动识别播放源 URL 并转换为官方 App 的专属协议：
    - **哔哩哔哩**：优先转换为 `bilibili://bangumi/season/<id>` 或视频页 Intent。
    - **巴哈姆特动画疯**：转换为 `animegamer://animedetail?sn=<sn>`。
    - **爱奇艺/腾讯视频**：对应原生包名及播放页 Intent。
- **智能探测与平滑降级**：
  - 检测用户手机中是否已安装对应的官方客户端（通过 `PackageManager.queryIntentActivities`）。
  - 若已安装：**一秒唤醒官方 App 直达对应剧集播放**！
  - 若未安装：弹出友好提示：“未安装哔哩哔哩客户端，是否使用浏览器打开？”，降级使用 Chrome Custom Tabs。

#### 3. 涉及模块与代码范围
- `:core:common` (新增 `intent/StreamingAppLauncher.kt`)
- `:feature:schedule` (`ScheduleSourcesBottomSheet.kt`)
- `:feature:subject` (`SubjectDetailScreen.kt`)

---

### 🏥 战役 03：日程主页“主客倒置”信息过载治理

#### 1. 病症表现
- 用户每天打开 MiniBgm 的第一心理诉求是：**“今天我追的番更了没？更在什么时候？”**
- 但目前的日程首页默认将全网今天开播的全部 30~50 部番剧一视同仁地平铺展现。用户必须在漫长的列表中不断滑动，用肉眼人肉检索自己的番剧，主次严重颠倒。

#### 2. 治疗处方
- **首页顶部常驻双模式胶囊切换**：
  - 【**我的追番**】(默认优先展示，记忆用户偏好) vs 【**全网开播**】。
- **“我的追番”无缝体验**：
  - 当处于“我的追番”模式时，仅筛选出当前登录用户标记为“在看 / 想看”且今日有放送的条目。
  - 若今日用户追的番暂未更新，呈现温馨空状态卡片：“今天没有你追的番更新哦，去【全网开播】看看新番吧”。
- **全网开播作为探索与补番入口**：
  - 只有当用户主动切换到“全网开播”时，才加载当天的全网大盘数据。

#### 3. 涉及模块与代码范围
- `:feature:schedule` (`ScheduleScreen.kt`, `ScheduleViewModel.kt`)
- `:core:datastore` (`UserPreferences` 记录用户上次选择的日程视图偏好)

---

### 🏥 战役 04：卡片视觉降噪与内容沉浸化

#### 1. 病症表现
- 诸如 `NextUpActionCard` 等卡片大面积使用高对比度的 `primaryContainer` 和鲜艳 Emoji 标签，抢夺了番剧立绘海报的视觉焦点。
- 卡片尺寸规格不一，有的撑满大半个屏幕，导致首屏有效信息密度极低。

#### 2. 治疗处方
- **严格遵循 Content-First（内容第一，框架后退）**：
  - 容器统一采用 `MaterialTheme.colorScheme.surfaceContainerLow`，搭配微透明浅描边（`outlineVariant.copy(0.2f)`）。
  - 海报统一遵循 2:3 黄金比例，圆角统一为 `12.dp`。
- **条目卡片紧凑化**：
  - 列表行高度严格控制在 56~64dp，杜绝无效空白与多余层级。
  - 去除粗糙的大号 Emoji，改用精致的 Material 3 矢量小图标。

#### 3. 涉及模块与代码范围
- `:core:designsystem` (统一定义卡片基类、间距与海报规格)
- `:feature:schedule` (`NextUpActionCard.kt`, `ScheduleTimelineComponents.kt`)
- `:feature:assistant` (`PendingActionCard.kt`)

---

### 🏥 战役 05：单集吐槽与讨论原生化（v0.4.0）

#### 1. 病症表现
- 动画看到精彩处或神转折，最核心的需求是看同好的吐槽与讨论。
- 当前只要点进讨论，就被重定向到浏览器加载 Bangumi 移动网页端，加载慢、字体小、无法点赞或快捷回复。

#### 2. 治疗处方
- **原生单集独立吐槽楼 (`EpisodeDetailScreen`)**：
  - 直接调用 Bangumi `/v0/episodes/{id}` 及单集评论接口，在 App 原生界面中以瀑布流形式渲染评论。
  - 支持 BBCode 原生解析（表情包、加粗、代码块、链接）。
- **剧透防爆遮罩 (Spoiler Shield)**：
  - 对用户标记为剧透的吐槽，默认使用模糊蒙层或“黑条涂抹”效果，点击后平滑淡出展示，保护补番党体验。

#### 3. 涉及模块与代码范围
- `:feature:subject` (`EpisodeDetailScreen.kt`, `SubjectCommunitySection.kt`)
- `:core:network` & `:core:data` (单集讨论接口与缓存)

---

### 🏥 战役 06：AI 助手页面级上下文感知 (Context-Aware)（v0.4.0）

#### 1. 病症表现
- 用户在《葬送的芙莉莲》条目页遇到疑问打开 AI 助手，AI 就像刚失忆一样，不知道用户在看哪部番。用户还得手动输入：“帮我查下芙莉莲下一集什么时候播”。

#### 2. 治疗处方
- **路由级上下文注入**：
  - 允许通过带参路由唤起 AI 助手：`BgmRoute.Assistant(subjectId = 12345, subjectName = "葬送的芙莉莲", currentProgress = 10)`。
- **首轮 Prompt 隐式注入**：
  - 助手初始化时，自动将当前页面的条目信息作为隐式上下文载入 Session。
  - 界面输入框给出贴心推荐 Chip：“这集讲了什么？”、“帮我打卡到最新一集”、“评分和口碑如何？”。

#### 3. 涉及模块与代码范围
- `:core:navigation` (`BgmRoutes.kt`)
- `:app` (`BgmNavState.kt`, `BgmNavHost.kt`)
- `:feature:assistant` (`AssistantViewModel.kt`, `AssistantScreen.kt`)

---

## 🎯 实施排期与验收 Checkbox

### 第一阶段：核心卡点立即治 (v0.3.0 专项)
- [x] **[01-A]** 单集长按触发“看到此集”批量打卡弹窗
- [x] **[01-B]** 打卡后 4 秒底部撤销 (Undo) 浮条与事务回滚
- [x] **[02-A]** 封装 `StreamingAppLauncher` 原生 Deep Link 探测与跳转
- [x] **[02-B]** 播放源优先唤醒哔哩哔哩 / 巴哈姆特客户端，未安装降级为 Chrome Custom Tabs
- [x] **[03-A]** 日程页顶部增加【我的追番 / 全部开播】切换过滤
- [x] **[03-B]** “我的追番”模式支持空状态温馨引导与偏好持久化记忆
- [x] **[04-A]** 全局卡片视觉降噪，统一规范为 `surfaceContainerLow` 与 2:3 海报比例

### 第二阶段：社区与智能体验原生化 (v0.4.0 专项)
- [ ] **[05-A]** 原生单集吐槽流与 BBCode 富文本渲染
- [ ] **[05-B]** 剧透吐槽点击展开遮罩机制
- [ ] **[06-A]** 条目详情页唤起 AI 助手携带番剧上下文
- [ ] **[06-B]** 助手智能推荐 Chip 与一键执行当前番剧打卡

### 第三阶段：容错与细微体验打磨 (v0.5.0 专项)
- [ ] **[07-A]** bangumi-data 别名字典本地索引与模糊容错搜索
- [ ] **[07-B]** 列表页与详情页返回时滚动位置严格记忆
- [ ] **[08-A]** 网络异常时结合 Room 离线缓存无感降级展现与局部重试
